


1、jquery 中的循环：

	（1）for 循环：

		var $input = $( 'input' );

		for( var i=0; i < $inputs.length; i++ )

	（2）for ... of 循环：

		var $input = $( 'input' );

		for( var  input  of  $inputs )


	遍历方法：each（）方法：

		$("li").each ( function ( ) {
    		         alert( $( this ). text( ) )
  		});











